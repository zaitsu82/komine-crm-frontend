# AGENTS.md

このファイルは **Codex / Claude Code / Cursor** など、コーディングエージェント共通の指示です。
ツール固有ファイル（`CLAUDE.md` / `.cursor/rules/`）より先に、ここを守ってください。

> 正本はワークスペース直下の `AGENTS.md`。各リポジトリにも同内容を置く。変更するときは全リポジトリの `AGENTS.md` を揃えてください。

---

## 変更前ゲート（必須・例外なし）

**ファイルを 1 つでも作成・編集・削除する前に**、必ず次を完了すること。ゲートを飛ばして編集を始めてはいけない。

1. 対象リポジトリで `git branch --show-current` と `git status -sb` を確認する
2. `main` / `master` / 本番相当ブランチ上、**または別目的の作業ブランチ上**なら、最新の本番相当ブランチから **新しい作業ブランチを切る**
3. 作業ブランチ上であることを確認してから、初めて編集を開始する

```bash
git checkout main && git pull
git checkout -b <prefix>/<短い説明>
```

読み取り・調査だけのときはブランチ不要。**書き込みの瞬間から必須**。

---

## Git ブランチ運用（必須）

変更を加える作業では、いつでも直前の状態に戻せるよう、ブランチ単位で小さく管理する。

### 基本ルール

1. **main（または既定の本番相当ブランチ）へ直接 commit / push しない**
2. **作業開始時（＝変更前）に必ずブランチを切る**（目的の違う作業ごとに新しいブランチ）
3. **小さく commit** し、区切りがついたら **remote へ push**
4. **完了したら PR を作成し、マージする**（「push したまま放置」にしない）
5. 次の作業は **マージ後の最新 main から新しいブランチ** で再開する

### ブランチの単位

- ✅ 1つのまとまった変更＝1ブランチ（機能・修正・調査・ドキュメントなど）
- ✅ 実験・戻す可能性がある作業は必ず別ブランチ
- ❌ 目的の違う変更を同一ブランチに混ぜない
- ❌ キー入力ごとなど過剰なブランチ分割は不要（同じ作業の続きは同一ブランチで commit）

### ブランチ名の例

- `feat/...` 機能追加
- `fix/...` バグ修正
- `docs/...` ドキュメント・説明文
- `chore/...` 設定・ルール・雑務

### Agent の振る舞い

- **編集ツールを呼ぶ前に** 変更前ゲートを実行する（後回し禁止）
- commit / push / PR 作成は **ユーザーが依頼したときのみ** 行う（依頼なしに勝手に push・マージしない）
- ユーザーが「コミットして」「PR作って」「マージして」と依頼したら、この運用に沿って実行する
- 変更完了の報告時は、未 push・未 PR なら「ブランチ push → PR → マージ」を次の一手として短く提示する

### 目的

こまめにブランチ・push・マージすることで、問題が起きたときに **その変更単位だけ戻せる** 状態を常に保つ。
