{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "git clone --depth 1 https://github.com/zaitsu82/komine-types.git ../packages/types && cd ../packages/types && npm ci && npm run build && cd - && next build",
  "framework": "nextjs"
}
